# Local development environment variables
# This is what AI assumes your environment looks like locally

NODE_ENV=development
PORT=3000

# Local database configuration (what AI typically generates)
DATABASE_URL=postgresql://localhost:5432/userservice

# Local Redis configuration (what AI assumes)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# External API configuration (what AI uses from documentation)
EXTERNAL_API_URL=https://api.example.com
API_KEY=your-api-key-here

# JWT configuration
JWT_SECRET=your-jwt-secret-here

# Note: Copy this to .env.local for local development
# Run: cp env-samples/local.env .env.local 